---
meta:
  - title: MDX Route
  - name: description
    content: This is an MDX Route!
headers:
  X-Custom-Header: This came from frontmatter!
---

import { json } from '@remix-run/node'
import { useLoaderData } from '@remix-run/react'

export const loader = () => {
  return json({ message: 'This is loader data.' })
}

export const Component = () => {
  const loaderData = useLoaderData()
  return (
    <>
      <p>This is a React component in an MDX route file.</p>
      <p>{loaderData.message}</p>
    </>
  )
}

# {attributes.meta[0].title}

This is an MDX route file.

<Component />
